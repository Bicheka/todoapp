import type { Request, Response } from "express"

export const getAllTodoLists = async () => {
    // get all todo lists
}

export const createTodoList = async (req: Request, res: Response) => {
// get all todo lists
}

// TODO: create there rest of controllers