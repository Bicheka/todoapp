{
	"name": "tudoo",
	"version": "1.0.0",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"format": "bunx biome check --write",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate"
	},
	"dependencies": {
		"@hono/zod-openapi": "^1.2.1",
		"@scalar/hono-api-reference": "^0.9.40",
		"drizzle-orm": "^0.45.1",
		"hono": "^4.11.7",

		"_comment:pg": "pg is needed as a driver for Drizzle because the Bun SQL driver is not fully supported yet",
		"pg": "^8.18.0",

		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.14",
		"@types/bun": "latest",
		"drizzle-kit": "^0.31.8"
	}
}
